package com.wilco375.roosternotification.general

object Config {
    // The amount of days that is synced
    // For example, if this value is 30, the application will sync all lessons
    // from 30 days back until 30 days in the future
    val SYNC_WINDOW = 30
}